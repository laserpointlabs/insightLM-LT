{
  "name": "workbook-rag",
  "description": "Vector search for workbook documents using OpenAI embeddings and LanceDB",
  "command": "python",
  "args": ["server.py"],
  "enabled": true,
  "env": {
    "OPENAI_API_KEY": "${OPENAI_API_KEY}"
  }
}
