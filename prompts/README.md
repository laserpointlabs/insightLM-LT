## Reusable AI “/commands” (copy/paste prompts)

This folder contains short, reusable prompts you can paste into a fresh AI thread to quickly align on our engineering standards.

### Available commands

- `/automation_smoke` → how we add deterministic, selector-only UI smoke coverage and validate with `npm run test:automation:prod`
