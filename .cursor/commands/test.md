DEPRECATED: placeholder command created for a quick example.

Use one of the real project commands instead (see `.cursor/commands/README.md`), for example:
- `/automation_smoke`
- `/automation_add_coverage`
- `/feature_dod`
